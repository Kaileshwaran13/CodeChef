#include <bits/stdc++.h>
using namespace std;

int main() {
    int tt, a, b;
    cin >> tt;
    while(tt--){
        cin >> a >> b;
        int prod = sqrt(a);
        cout << prod * b << '\n';
    }

}
