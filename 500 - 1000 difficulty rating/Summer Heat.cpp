#include <bits/stdc++.h>
using namespace std;

int main() {
    int tt, a, b, x, y;
    cin >> tt;
    while(tt--){
        cin >> a >> b >> x >> y;
        cout << (x / a) + (y / b) <<'\n';
    }

}
