#include <bits/stdc++.h>
using namespace std;

int main() {
    int T = 1;
    cin >> T;
    while (T--) {
        int l, r;
        cin >> l >> r;
        cout << (int) 1e9 + 7 << '\n';
    }
    return 0;
}
