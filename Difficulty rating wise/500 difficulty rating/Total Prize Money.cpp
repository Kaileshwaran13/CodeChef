#include <bits/stdc++.h>
using namespace std;

int main() {
    int tt, x, y;
    cin >> tt;
    while(tt--){
        cin >> x >> y;
        cout << (x * 10) + (y * 90) << '\n';
    }
	// your code goes here

}
